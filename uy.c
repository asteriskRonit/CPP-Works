#include<stdio.h>
#include<stdlib.h>
#include<greeting.h>
void main()
{
	/*int def,c=0;
	printf("Enter the number:");
	scanf("%d",&def);
	if(def>0)
	{
		for(int i=1;i<=def/2;i++)
		{
			if(def%i==0)
				c++;
		}	
		if(c==1)
		   printf("%d is a positive no and prime no\n",def);
		else
		   printf("%d is a positive no but not prime no\n",def);
	}	
	else
		printf("%d is not a positive no\n",def);*/
		
		jkb();



}
